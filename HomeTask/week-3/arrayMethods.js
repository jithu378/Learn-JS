let arr = [12,34,1,56];

arr.push(60); // insert element at the end of the array
console.log(arr);
arr.pop(); // delete element from the end of the array
console.log(arr);
arr.unshift(100); // insert element at the starting of the array
console.log(arr);
arr.shift(); // delete elemnt from the starting of the array
console.log(arr);

let fruits = ["Apple", "Mango", "Watermelon"]
let join = fruits.join(" - "); //converts array to string with the 
console.log(join);

fruits.reverse(); // reverse the array
console.log(fruits);

let split = join.split(" - "); // convert string to array
console.log(split);


let fruit = ["Apple", "Mango", "Watermelon"]


